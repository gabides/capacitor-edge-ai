import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Capacitor Edge AI',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
